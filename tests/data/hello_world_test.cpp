#include "libftpp/data/data_structures.hpp"

int main() 
{
    Pool A;
    DataBuffer B;
    DataPuffer P;

    helloWorld(A._prototype.x);
    std::cout << "===\n";
    helloWorld(A._prototype.getY());
    std::cout << "===\n";
    showData(B);
    std::cout << "===\n";
    showData(P);

    return (0);
}
